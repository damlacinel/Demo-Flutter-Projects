import 'package:flutter/material.dart';
import 'package:quiz_app/quiz.dart';


void main(){
  runApp(
    //MaterialApp does not display anything + hence
    //an additional widget needs to be added to it
    Quiz()
  );
}
